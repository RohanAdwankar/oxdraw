# Contributing to oxdraw
Welcome, new contributors and issue reporters!
Before submitting changes:
- Add test cases for changed behavior.
- Rebuild the browser WASM.
- Use `view_visual_diff` to visually inspect every rendering diff.
